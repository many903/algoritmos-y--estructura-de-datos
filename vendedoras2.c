#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// Estructura para almacenar el nombre y las ventas de cada vendedor
typedef struct {
    char nombre[20];
    int ventas;
} Vendedor[n];

// Función que compara dos vendedores según sus ventas
int comparacion (){

}

int lee_vendedores(){
    
}
int main() {
    /*lee cuantos son (n) y cuantos tengo que dar(k)*/
    int n, k;
    n=k=0;
    scanf("%d %d", &n, &k);
    /*lee vendedores*/
    if n>0{
        lee_vendedores();
    }
    return 0;
}